[Unit]
Description=xtesting@kibana.service

[Service]
User={{ ansible_user | default(lookup('env', 'USER')) }}
ExecStart=/usr/local/bin/kubectl port-forward service/kibana-kibana {{ kibana_port }}:5601 --address 0.0.0.0

[Install]
WantedBy=multi-user.target
